<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<%= link_to "Edit Post", edit_post_path_url(@post), class: 'btn btn-default' %>
<%= link_to "Back to All Posts", posts_path_url, class: 'btn btn-default' %>

