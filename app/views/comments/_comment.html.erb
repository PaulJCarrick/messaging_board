<div class="comment">
  <p class="comment-content">
    <%=comment.body%>
  </p>
  <p class="comment-posted-by">Posted by <%= comment.user.name %> at <%= time_ago_in_words(comment.created_at) %> ago.</p>
  <div class="links">
    <% if comment.user_id == current_user.try(:id) %>
      <%= link_to 'Edit',   edit_post_comment_path(comment.post, comment) %>
      <%= link_to 'Delete', post_comment_path(comment.post, comment), method: :delete, data: { confirm: 'Do you want to delete this comment?' } %>
    <% end %>
  </div>
</div>